// Tailwind v4 uses content-aware engine by default; config is optional.
// Keeping minimal config for future customizations.
export default {
  content: ['./index.html', './src/**/*.{ts,tsx,js,jsx}'],
};

